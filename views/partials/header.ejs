<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <!-- SEO Meta Tags -->
  <meta name="description" content="Join Dr. Aditi Pajiyar for a free webinar on student mental health. Learn practical strategies to thrive beyond grades and manage academic stress.">
  <meta name="keywords" content="mental health, student wellness, academic stress, webinar, Dr. Aditi Pajiyar">

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website">
  <meta property="og:url" content="<%= typeof siteUrl !== 'undefined' ? siteUrl : '' %>">
  <meta property="og:title" content="Student Mental Health: Thriving Beyond Grades - Free Webinar">
  <meta property="og:description" content="Feeling overwhelmed by exams and expectations? Join Dr. Aditi Pajiyar for an honest conversation about student mental health and practical strategies to thrive. Free registration!">
  <meta property="og:image" content="<%= typeof siteUrl !== 'undefined' ? siteUrl : '' %>/og-image.png">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:image:type" content="image/png">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:url" content="<%= typeof siteUrl !== 'undefined' ? siteUrl : '' %>">
  <meta name="twitter:title" content="Student Mental Health: Thriving Beyond Grades - Free Webinar">
  <meta name="twitter:description" content="Feeling overwhelmed by exams and expectations? Join Dr. Aditi Pajiyar for an honest conversation about student mental health. Free registration!">
  <meta name="twitter:image" content="<%= typeof siteUrl !== 'undefined' ? siteUrl : '' %>/og-image.png">

  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- Flash Messages -->
  <% if (success_msg && success_msg.length > 0) { %>
    <div class="alert alert-success">
      <%= success_msg %>
    </div>
  <% } %>
  <% if (error_msg && error_msg.length > 0) { %>
    <div class="alert alert-error">
      <%= error_msg %>
    </div>
  <% } %>
  <% if (error && error.length > 0) { %>
    <div class="alert alert-error">
      <%= error %>
    </div>
  <% } %>
